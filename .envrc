# Flywall Project Environment
# Powered by direnv

# Add project-specific binaries to PATH
PATH_add local/bin

# Set project root for scripts
export FLYWALL_ROOT=$PWD

echo "✔ Flywall environment loaded. Use 'fw help' for commands."
